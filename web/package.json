{
  "name": "healthwewear",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Seguimiento de peso en grupo sobre una hoja de Google Sheets",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "publicar": "vite build --outDir ../docs --emptyOutDir && node -e \"require('fs').writeFileSync('../docs/.nojekyll','')\""
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
